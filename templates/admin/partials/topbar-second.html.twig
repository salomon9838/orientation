{# templates/admin/partials/topbar-second.html.twig #}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm fixed-top">
    <div class="container-fluid px-4">

        <!-- Logo -->
        <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="#">
            🎓 SchoolPrepar
            <span class="badge bg-primary">Admin</span>
        </a>

        <!-- Toggle mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#topNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Menu -->
        <div class="collapse navbar-collapse" id="topNavbar">

            <!-- Liens -->
            <ul class="navbar-nav me-auto ms-4 gap-3">

                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_etablissements' ? 'active' : '' }}"
                       href="{{ path('app_etablissements_index') }}">
                        <i class="ri-dashboard-line me-1"></i> Dashboard
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_filieres' ? 'active' : '' }}"
                       href="{{ path('app_filieres_index') }}">
                        <i class="ri-book-open-line me-1"></i> Filières
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_etablissements' ? 'active' : '' }}"
                       href="{{ path('app_etablissements_index') }}">
                        <i class="ri-building-line me-1"></i> Établissements
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_evenement' ? 'active' : '' }}"
                       href="{{ path('app_evenement_index') }}">
                        <i class="ri-calendar-event-line me-1"></i> Événements
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_utilisateur' ? 'active' : '' }}"
                       href="{{ path('app_utilisateur_index') }}">
                        <i class="ri-user-3-line me-1"></i> Utilisateurs
                    </a>
                </li>

            </ul>

            <!-- Actions droite -->
            <div class="d-flex align-items-center gap-3">

                <!-- Recherche -->
                <button class="btn btn-outline-light btn-sm" data-bs-toggle="modal" data-bs-target="#searchModal">
                    <i class="ri-search-line"></i>
                </button>

                <!-- Notifications -->
                <div class="position-relative">
                    <button class="btn btn-outline-light btn-sm">
                        <i class="ri-notification-3-line"></i>
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            3
                        </span>
                    </button>
                </div>

                <!-- Profil -->
                <div class="d-flex align-items-center gap-2">
                    <div class="text-end">
                        <div class="fw-semibold small text-white">Admin</div>
                        <div class="text-muted small">Super Admin</div>
                    </div>
                    <img src="{{ asset('assets/images/avatar/avatar-1.jpg') }}"
                         class="rounded-circle border border-light"
                         width="35" height="35">
                </div>

                <!-- Logout -->
                <a href="{{ path('app_logout') }}" class="btn btn-outline-warning btn-sm">
                    <i class="ri-logout-box-line"></i>
                </a>

            </div>

        </div>
    </div>
</nav>