{% extends 'base.html.twig' %}

{% block title %}Edit Filiere{% endblock %}

{% block stylesheets %}
    {{ parent() }}

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
{% endblock %}

{% block body %}
<div class="container py-5">

    <div class="card shadow-sm border-0">
        <div class="card-body">

            <h1 class="text-center fw-bold mb-4">Edit Filiere</h1>

            <div class="mb-4">
                {{ include('fil/_form.html.twig', {'button_label': 'Update'}) }}
            </div>

            <div class="d-flex justify-content-between mt-4">
                <a href="{{ path('app_filieres_index') }}" class="btn btn-outline-secondary">
                    ← Retour à la liste
                </a>

                {{ include('fil/_delete_form.html.twig') }}
            </div>

        </div>
    </div>

</div>
{% endblock %}