{% extends 'base.html.twig' %}

{% block title %}Éditer la filière - SchoolPrepar{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="display-6 fw-bold text-primary">
                <i class="fa fa-edit"></i> Éditer la filière
            </h1>
            <p class="text-muted">{{ filiere.nom }}</p>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-8">
            {% include 'filiere/_form.html.twig' with {
                'form': form,
                'button_label': 'Mettre à jour'
            } %}
        </div>
    </div>
</div>
{% endblock %}
